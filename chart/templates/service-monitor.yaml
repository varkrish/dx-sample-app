---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "dx-address-extract-nzad.fullname" . }}
  labels:
    {{- include "dx-address-extract-nzad.labels" . | nindent 4 }}
spec:
  endpoints:
    - interval: 30s
      port: tcp-8080
      scheme: http
  selector:
    matchLabels:
    {{- include "dx-address-extract-nzad.labels" . | nindent 6 }}
